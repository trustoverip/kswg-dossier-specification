[[def: observation-attestation, observation attestation]]

~ A signed ACDC produced during [[ref: temporal-pinning]] that records a dynamic value observed at a specific time — for example, an account balance at a given block height. It freezes volatile data into a static, timestamped snapshot that a dossier can link to, allowing the dossier to assert what was true at the moment of assembly rather than what is true now.
