[[def: reference-time, referenceTime, reference time]]

~ An input to the verification algorithm specifying the point in time at which a dossier's signatures, key state, and revocation status are evaluated. Because KERI preserves historical key state, a reference time lets a verifier determine whether a dossier was valid as of an arbitrary past moment, not only the present.
