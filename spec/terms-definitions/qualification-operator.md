[[def: qualification-operator, qualification operator]]

~ An operator, `Q`, that determines a standard of proof which signers MUST meet. When this operator is present, the edge MUST also contain a `qschema` property describing the proof that must exist, plus a `qev` array that enumerates the edges of evidence each signer presents as proof of qualification.
