[[def: oracle, oracles, observer, oracle service]]

~ A trusted role (also called the observer) that queries a dynamic data source at a specific time and wraps the result in an [[ref: observation-attestation]]. The oracle enables a dossier to reference volatile state as a fixed, verifiable snapshot; it may be the assembler itself or a trusted third-party service.
